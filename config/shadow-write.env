# ===== TaskDB Drivers =====
TASKDB_DRIVER=dual                # dual = shadow write (sqlite + pg)
TASKDB_SQLITE_PATH=./data/taskdb.sqlite
TASKDB_PG_URL=postgres://user:pass@localhost:5432/taskdb

# dual-mode behaviour
TASKDB_DUAL_STRICT=false          # false = no falla si una rama falla
TASKDB_DUAL_LOG_MISMATCH=true     # loggear diferencias de conteo

# ===== Metrics Exporter =====
METRICS_PORT=9464                 # puerto HTTP para /metrics
METRICS_PREFIX=taskdb_            # prefijo de métricas